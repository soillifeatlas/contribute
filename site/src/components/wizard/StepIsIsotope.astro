---
import WizardFrame from './WizardFrame.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<WizardFrame title="IS / isotope" currentStep={7} prevHref={`${base}/submit/biomass/`} nextHref={`${base}/submit/extraction/`}>
  <p class="mono-label">Step 07 · 9</p>
  <h2 class="font-display text-[1.25rem] mt-1">IS / isotope</h2>
  <p class="mt-4 text-muted">Content coming in Phase 7.</p>
</WizardFrame>
