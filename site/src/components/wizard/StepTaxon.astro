---
import WizardFrame from './WizardFrame.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<WizardFrame title="Taxon" currentStep={4} prevHref={`${base}/submit/tier/`} nextHref={`${base}/submit/sample/`}>
  <p class="mono-label">Step 04 · 9</p>
  <h2 class="font-display text-[1.25rem] mt-1">Taxon</h2>
  <p class="mt-4 text-muted">Content coming in Phase 7.</p>
</WizardFrame>
