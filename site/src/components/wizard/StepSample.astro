---
import WizardFrame from './WizardFrame.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<WizardFrame title="Sample" currentStep={5} prevHref={`${base}/submit/taxon/`} nextHref={`${base}/submit/biomass/`}>
  <p class="mono-label">Step 05 · 9</p>
  <h2 class="font-display text-[1.25rem] mt-1">Sample</h2>
  <p class="mt-4 text-muted">Content coming in Phase 7.</p>
</WizardFrame>
